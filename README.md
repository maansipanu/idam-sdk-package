To log in to the npm registry, add an entry in .npmrc 
then
Open Powershell and then set- $env:GITHUB_TOKEN="your_personal_access_token_here"
to confirm type- $env:GITHUB_TOKEN
then
type- npm publish
